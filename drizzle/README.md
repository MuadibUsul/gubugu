# Drizzle Workspace

Place future database assets here:

- `schema/` for typed table definitions
- `migrations/` for generated SQL migrations
- `seed/` for idempotent seed scripts

Keep every schema change migration-driven.
